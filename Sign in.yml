info:
  name: Sign in
  type: http
  seq: 1

http:
  method: POST
  url: http://hoople.events.api/signin/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
