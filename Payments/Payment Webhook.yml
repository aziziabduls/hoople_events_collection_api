info:
  name: Payment Webhook
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/webhooks/payment"
  auth: inherit
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "eventType": "payment.succeeded",
        "deliveryId": "evt-0a1b2c3d-1111-4222-8333-000000000801",
        "occurredAt": "2026-03-14T09:41:00+07:00",
        "data": {
          "paymentId": "pay-9f8e7d6c-1111-4222-8333-000000000701",
          "bookingId": "b0071e5c-1111-4d2e-8a3f-000000000301",
          "status": "paid",
          "amount": 251520,
          "currency": "IDR",
          "reference": "TRX-BCA-260314-77120"
        },
        "signature": "sha256=6b8f0c2a1d4e5f7890abcdef1234567890abcdef1234567890abcdef12345678"
      }

runtime:
  assertions:
    - expression: res.status
      operator: lt
      value: "400"
    - expression: res.body.success
      operator: eq
      value: "true"

docs: |-
  # Payment Webhook

  Callback dari payment gateway ke backend.

  | | |
  | --- | --- |
  | **Method** | `POST` |
  | **Path** | `/webhooks/payment` |
  | **Auth** | Wajib — `Authorization: Bearer <accessToken>` |
  | **Sukses** | `200 OK` |
  | **Sumber** | `dto/payments/payment-webhook.request.json` |

  ## Request body

  `application/json`

  | Field | Tipe | Wajib | Catatan |
  | --- | --- | --- | --- |
  | `eventType` | string | ya |  |
  | `deliveryId` | string | ya |  |
  | `occurredAt` | string | ya | ISO 8601 `+07:00` |
  | `data` | object | ya |  |
  | `data.paymentId` | string | ya |  |
  | `data.bookingId` | string | ya | UUID v4 |
  | `data.status` | string | ya | enum `bookingTimelineStatus`: `created`, `paid`, `confirmed`, `cancelled`, `refunded` |
  | `data.amount` | integer | ya |  |
  | `data.currency` | string | ya |  |
  | `data.reference` | string | ya |  |
  | `signature` | string | ya |  |


  > Kolom **Wajib** diturunkan dari contoh: field yang bernilai `null`
  > dianggap opsional. Aturan validasi sesungguhnya ditentukan backend.

  ## Error yang mungkin

  | Kode | Contoh | Kapan |
  | --- | --- | --- |
  | `401` | `common/error-unauthorized.response.json` | Token tidak ada atau kedaluwarsa. |
  | `422` | `common/error-validation.response.json` | Validasi gagal; pesan per field ada di `error.errors`. |
  | `500` | `common/error-server.response.json` | Kesalahan tak terduga. |

  Semua error memakai envelope yang sama ditambah blok `error`:

  ```json
  { "success": false, "message": "…", "data": null, "meta": null,
    "error": { "code": "VALIDATION_ERROR", "errors": { "email": ["…"] } } }
  ```

  ## Konvensi yang berlaku

  - **Nama field** camelCase.
  - **Uang** di response berbentuk `{ amount: integer, currency: "IDR" }`; di request integer polos.
  - **Instant** (`createdAt`, `paidAt`, `startsAt`, `expiresAt`) ISO 8601 `+07:00`. Jam sesi berulang tetap `"HH:mm"`.
  - **Enum** lowercase. Label berhuruf besar adalah urusan frontend.
  - **Field nullable** tetap dikirim sebagai `null`, jangan dihilangkan.
  - **`roles`** adalah array — satu akun bisa `["participant", "organizer"]`.

  Selengkapnya di `dto/README.md`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
