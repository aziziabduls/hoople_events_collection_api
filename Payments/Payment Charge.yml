info:
  name: Payment Charge
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/payments/charge"
  auth: inherit
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "bookingId": "b0071e5c-1111-4d2e-8a3f-000000000301",
        "paymentMethodId": "pm-bca-va"
      }

runtime:
  assertions:
    - expression: res.status
      operator: lt
      value: "400"
    - expression: res.body.success
      operator: eq
      value: "true"

examples:
  - name: Sukses
    response:
      status: 201
      statusText: Created
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": true,
            "message": "Instruksi pembayaran berhasil dibuat",
            "data": {
              "paymentId": "pay-9f8e7d6c-1111-4222-8333-000000000701",
              "bookingId": "b0071e5c-1111-4d2e-8a3f-000000000301",
              "status": "pending",
              "methodId": "pm-bca-va",
              "methodName": "BCA Virtual Account",
              "amount": {
                "amount": 251520,
                "currency": "IDR"
              },
              "virtualAccountNumber": "8277 0812 3456 7890",
              "qrString": null,
              "deeplinkUrl": null,
              "expiresAt": "2026-03-14T10:30:00+07:00",
              "instructions": [
                {
                  "channel": "ATM BCA",
                  "steps": [
                    "Masukkan kartu dan PIN",
                    "Pilih Transaksi Lainnya > Transfer > ke BCA Virtual Account",
                    "Masukkan nomor 8277 0812 3456 7890",
                    "Konfirmasi jumlah Rp 251.520"
                  ]
                },
                {
                  "channel": "m-BCA",
                  "steps": [
                    "Buka m-BCA > m-Transfer > BCA Virtual Account",
                    "Masukkan nomor 8277 0812 3456 7890",
                    "Konfirmasi dan masukkan PIN"
                  ]
                }
              ]
            },
            "meta": null
          }

docs: |-
  # Payment Charge

  Terbitkan instruksi bayar (nomor VA, QR, deeplink).

  | | |
  | --- | --- |
  | **Method** | `POST` |
  | **Path** | `/payments/charge` |
  | **Auth** | Wajib — `Authorization: Bearer <accessToken>` |
  | **Sukses** | `201 Created` |
  | **Sumber** | `dto/payments/payment-charge.request.json`<br>`dto/payments/payment-charge.response.json` |

  ## Request body

  `application/json`

  | Field | Tipe | Wajib | Catatan |
  | --- | --- | --- | --- |
  | `bookingId` | string | ya | UUID v4 |
  | `paymentMethodId` | string | ya |  |


  > Kolom **Wajib** diturunkan dari contoh: field yang bernilai `null`
  > dianggap opsional. Aturan validasi sesungguhnya ditentukan backend.

  ## Response `201`

  Envelope standar:

  ```json
  { "success": true, "message": "…", "data": …, "meta": null }
  ```

  Isi `data`:

  | Field | Tipe | Nullable | Catatan |
  | --- | --- | --- | --- |
  | `paymentId` | string | tidak |  |
  | `bookingId` | string | tidak | UUID v4 |
  | `status` | string | tidak | enum `bookingStatus`: `pending`, `confirmed`, `completed`, `cancelled`, `refunded` |
  | `methodId` | string | tidak |  |
  | `methodName` | string | tidak |  |
  | `amount` | money | tidak | `{ amount: integer, currency: "IDR" }` |
  | `virtualAccountNumber` | string | tidak |  |
  | `qrString` | — | ya | selalu `null` di contoh — tipe ditentukan backend |
  | `deeplinkUrl` | — | ya | selalu `null` di contoh — tipe ditentukan backend |
  | `expiresAt` | string | tidak | ISO 8601 `+07:00` |
  | `instructions` | object[] | tidak |  |
  | `instructions[].channel` | string | tidak |  |
  | `instructions[].steps` | string[] | tidak |  |

  ## Error yang mungkin

  | Kode | Contoh | Kapan |
  | --- | --- | --- |
  | `401` | `common/error-unauthorized.response.json` | Token tidak ada atau kedaluwarsa. |
  | `422` | `common/error-validation.response.json` | Validasi gagal; pesan per field ada di `error.errors`. |
  | `500` | `common/error-server.response.json` | Kesalahan tak terduga. |

  Semua error memakai envelope yang sama ditambah blok `error`:

  ```json
  { "success": false, "message": "…", "data": null, "meta": null,
    "error": { "code": "VALIDATION_ERROR", "errors": { "email": ["…"] } } }
  ```

  ## Konvensi yang berlaku

  - **Nama field** camelCase.
  - **Uang** di response berbentuk `{ amount: integer, currency: "IDR" }`; di request integer polos.
  - **Instant** (`createdAt`, `paidAt`, `startsAt`, `expiresAt`) ISO 8601 `+07:00`. Jam sesi berulang tetap `"HH:mm"`.
  - **Enum** lowercase. Label berhuruf besar adalah urusan frontend.
  - **Field nullable** tetap dikirim sebagai `null`, jangan dihilangkan.
  - **`roles`** adalah array — satu akun bisa `["participant", "organizer"]`.

  Selengkapnya di `dto/README.md`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
