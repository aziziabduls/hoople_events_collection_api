info:
  name: Payment Status
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/payments/{paymentId}"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: lt
      value: "400"
    - expression: res.body.success
      operator: eq
      value: "true"

examples:
  - name: Sukses
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": true,
            "message": "Status pembayaran berhasil diambil",
            "data": {
              "paymentId": "pay-9f8e7d6c-1111-4222-8333-000000000701",
              "bookingId": "b0071e5c-1111-4d2e-8a3f-000000000301",
              "status": "paid",
              "amount": {
                "amount": 251520,
                "currency": "IDR"
              },
              "expiresAt": null,
              "paidAt": "2026-03-14T09:41:00+07:00",
              "reference": "TRX-BCA-260314-77120",
              "failureReason": null
            },
            "meta": null
          }

docs: |-
  # Payment Status

  Polling status bayar.

  | | |
  | --- | --- |
  | **Method** | `GET` |
  | **Path** | `/payments/{paymentId}` |
  | **Auth** | Wajib — `Authorization: Bearer <accessToken>` |
  | **Sukses** | `200 OK` |
  | **Sumber** | `dto/payments/payment-status.response.json` |

  ## Path parameter

  | Nama | Catatan |
  | --- | --- |
  | `{paymentId}` | Lihat contoh di URL. |

  ## Request body

  Tidak ada.

  ## Response `200`

  Envelope standar:

  ```json
  { "success": true, "message": "…", "data": …, "meta": null }
  ```

  Isi `data`:

  | Field | Tipe | Nullable | Catatan |
  | --- | --- | --- | --- |
  | `paymentId` | string | tidak |  |
  | `bookingId` | string | tidak | UUID v4 |
  | `status` | string | tidak | enum `bookingTimelineStatus`: `created`, `paid`, `confirmed`, `cancelled`, `refunded` |
  | `amount` | money | tidak | `{ amount: integer, currency: "IDR" }` |
  | `expiresAt` | string | ya | boleh `null` |
  | `paidAt` | string | tidak | ISO 8601 `+07:00` |
  | `reference` | string | tidak |  |
  | `failureReason` | — | ya | selalu `null` di contoh — tipe ditentukan backend |

  ## Error yang mungkin

  | Kode | Contoh | Kapan |
  | --- | --- | --- |
  | `401` | `common/error-unauthorized.response.json` | Token tidak ada atau kedaluwarsa. |
  | `404` | `common/error-not-found.response.json` | Id atau slug tidak ditemukan. |
  | `500` | `common/error-server.response.json` | Kesalahan tak terduga. |

  Semua error memakai envelope yang sama ditambah blok `error`:

  ```json
  { "success": false, "message": "…", "data": null, "meta": null,
    "error": { "code": "VALIDATION_ERROR", "errors": { "email": ["…"] } } }
  ```

  ## Konvensi yang berlaku

  - **Nama field** camelCase.
  - **Uang** di response berbentuk `{ amount: integer, currency: "IDR" }`; di request integer polos.
  - **Instant** (`createdAt`, `paidAt`, `startsAt`, `expiresAt`) ISO 8601 `+07:00`. Jam sesi berulang tetap `"HH:mm"`.
  - **Enum** lowercase. Label berhuruf besar adalah urusan frontend.
  - **Field nullable** tetap dikirim sebagai `null`, jangan dihilangkan.
  - **`roles`** adalah array — satu akun bisa `["participant", "organizer"]`.

  Selengkapnya di `dto/README.md`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
