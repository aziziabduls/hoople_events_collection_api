info:
  name: Checkin Summary
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/teams/check-in/summary?eventId=f00d1e00-1111-4222-8333-000000000601"
  auth: inherit
  params:
    - name: eventId
      value: f00d1e00-1111-4222-8333-000000000601
      type: query
      description: UUID event internal.

runtime:
  assertions:
    - expression: res.status
      operator: lt
      value: "400"
    - expression: res.body.success
      operator: eq
      value: "true"

examples:
  - name: Sukses
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": true,
            "message": "Ringkasan check-in berhasil diambil",
            "data": {
              "eventId": "f00d1e00-1111-4222-8333-000000000601",
              "eventTitle": "Annual Kick-off 2026",
              "registered": 412,
              "checkedIn": 247,
              "noShow": 0,
              "pending": 165,
              "checkInRate": 0.599,
              "scanners": [
                {
                  "id": "gate-1",
                  "label": "Gate 1 — Lobby",
                  "scans": 138,
                  "lastScanAt": "2026-03-20T09:15:00+07:00"
                },
                {
                  "id": "gate-2",
                  "label": "Gate 2 — Ballroom",
                  "scans": 109,
                  "lastScanAt": "2026-03-20T09:14:00+07:00"
                }
              ]
            },
            "meta": null
          }

docs: |-
  # Checkin Summary

  Progres per scanner.

  | | |
  | --- | --- |
  | **Method** | `GET` |
  | **Path** | `/teams/check-in/summary` |
  | **Auth** | Wajib — `Authorization: Bearer <accessToken>` |
  | **Sukses** | `200 OK` |
  | **Sumber** | `dto/teams/checkin-summary.response.json` |

  ## Query parameter

  | Nama | Contoh | Catatan |
  | --- | --- | --- |
  | `eventId` | `f00d1e00-1111-4222-8333-000000000601` | UUID event internal. |

  ## Request body

  Tidak ada.

  ## Response `200`

  Envelope standar:

  ```json
  { "success": true, "message": "…", "data": …, "meta": null }
  ```

  Isi `data`:

  | Field | Tipe | Nullable | Catatan |
  | --- | --- | --- | --- |
  | `eventId` | string | tidak | UUID v4 |
  | `eventTitle` | string | tidak |  |
  | `registered` | integer | tidak |  |
  | `checkedIn` | integer | tidak |  |
  | `noShow` | integer | tidak |  |
  | `pending` | integer | tidak |  |
  | `checkInRate` | number | tidak |  |
  | `scanners` | object[] | tidak |  |
  | `scanners[].id` | string | tidak |  |
  | `scanners[].label` | string | tidak |  |
  | `scanners[].scans` | integer | tidak |  |
  | `scanners[].lastScanAt` | string | tidak | ISO 8601 `+07:00` |

  ## Error yang mungkin

  | Kode | Contoh | Kapan |
  | --- | --- | --- |
  | `401` | `common/error-unauthorized.response.json` | Token tidak ada atau kedaluwarsa. |
  | `403` | `common/error-forbidden.response.json` | Akun tidak punya peran untuk konsol ini. |
  | `500` | `common/error-server.response.json` | Kesalahan tak terduga. |

  Semua error memakai envelope yang sama ditambah blok `error`:

  ```json
  { "success": false, "message": "…", "data": null, "meta": null,
    "error": { "code": "VALIDATION_ERROR", "errors": { "email": ["…"] } } }
  ```

  ## Konvensi yang berlaku

  - **Nama field** camelCase.
  - **Uang** di response berbentuk `{ amount: integer, currency: "IDR" }`; di request integer polos.
  - **Instant** (`createdAt`, `paidAt`, `startsAt`, `expiresAt`) ISO 8601 `+07:00`. Jam sesi berulang tetap `"HH:mm"`.
  - **Enum** lowercase. Label berhuruf besar adalah urusan frontend.
  - **Field nullable** tetap dikirim sebagai `null`, jangan dihilangkan.
  - **`roles`** adalah array — satu akun bisa `["participant", "organizer"]`.

  Selengkapnya di `dto/README.md`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
