info:
  name: Plans
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/org/subscription/plans"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
