info:
  name: Public Detail Lookup
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/events/google-dev-flutter-indonesia"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
