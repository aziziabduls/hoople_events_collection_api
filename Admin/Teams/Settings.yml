info:
  name: Settings
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/teams/settings"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: lt
      value: "400"
    - expression: res.body.success
      operator: eq
      value: "true"

examples:
  - name: Sukses
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": true,
            "message": "Pengaturan organisasi berhasil diambil",
            "data": {
              "organization": {
                "id": "d0d0d0d0-bbbb-4ccc-8ddd-000000000501",
                "name": "Sinar Nusantara",
                "legalName": "PT Sinar Nusantara Digital",
                "initials": "SN",
                "handle": "sinar-nusantara",
                "domain": "sinarnusantara.co.id",
                "plan": "organization",
                "memberCount": 486,
                "city": "Jakarta Selatan"
              },
              "membership": {
                "rule": "email_domain",
                "allowedDomains": [
                  "sinarnusantara.co.id"
                ],
                "requireCompanySignIn": true,
                "autoApprove": true
              },
              "departments": [
                {
                  "id": "dep-01",
                  "name": "Engineering",
                  "headcount": 164
                },
                {
                  "id": "dep-02",
                  "name": "Product & Design",
                  "headcount": 78
                },
                {
                  "id": "dep-03",
                  "name": "Sales & Growth",
                  "headcount": 96
                }
              ],
              "collections": {
                "bankName": "BCA",
                "accountNumber": "****4821",
                "accountHolder": "PT Sinar Nusantara Digital",
                "disbursement": "after_event",
                "platformFeeRate": 0,
                "payoutSchedule": "weekly",
                "isVerified": true
              },
              "admins": [
                {
                  "id": "3f8a1c22-9d41-4b7e-8f0a-11c2d3e4f501",
                  "name": "Adriani Ajeng",
                  "email": "adriani.ajeng@sinarnusantara.co.id",
                  "role": "owner",
                  "addedAt": "2025-11-02T09:00:00+07:00"
                },
                {
                  "id": "9c4d6f13-2a75-4e90-b3c1-44a5b6c7d203",
                  "name": "Raka Mahendra",
                  "email": "raka.mahendra@sinarnusantara.co.id",
                  "role": "admin",
                  "addedAt": "2026-01-15T10:30:00+07:00"
                }
              ]
            },
            "meta": null
          }

docs: |-
  # Settings

  Organisasi, aturan keanggotaan, rekening, admin.

  | | |
  | --- | --- |
  | **Method** | `GET` |
  | **Path** | `/teams/settings` |
  | **Auth** | Wajib — `Authorization: Bearer <accessToken>` |
  | **Sukses** | `200 OK` |
  | **Sumber** | `dto/teams/settings.response.json` |

  ## Request body

  Tidak ada.

  ## Response `200`

  Envelope standar:

  ```json
  { "success": true, "message": "…", "data": …, "meta": null }
  ```

  Isi `data`:

  | Field | Tipe | Nullable | Catatan |
  | --- | --- | --- | --- |
  | `organization` | object | tidak |  |
  | `organization.id` | string | tidak | UUID v4 |
  | `organization.name` | string | tidak |  |
  | `organization.legalName` | string | tidak |  |
  | `organization.initials` | string | tidak |  |
  | `organization.handle` | string | tidak |  |
  | `organization.domain` | string | tidak |  |
  | `organization.plan` | string | tidak |  |
  | `organization.memberCount` | integer | tidak |  |
  | `organization.city` | string | tidak |  |
  | `membership` | object | tidak |  |
  | `membership.rule` | string | tidak | enum `membershipRule`: `email_domain`, `invite_only`, `manual` |
  | `membership.allowedDomains` | string[] | tidak |  |
  | `membership.requireCompanySignIn` | boolean | tidak |  |
  | `membership.autoApprove` | boolean | tidak |  |
  | `departments` | object[] | tidak |  |
  | `departments[].id` | string | tidak |  |
  | `departments[].name` | string | tidak |  |
  | `departments[].headcount` | integer | tidak |  |
  | `collections` | object | tidak |  |
  | `collections.bankName` | string | tidak |  |
  | `collections.accountNumber` | string | tidak |  |
  | `collections.accountHolder` | string | tidak |  |
  | `collections.disbursement` | string | tidak | enum `disbursement`: `after_event`, `weekly`, `manual` |
  | `collections.platformFeeRate` | integer | tidak |  |
  | `collections.payoutSchedule` | string | tidak | enum `disbursement`: `after_event`, `weekly`, `manual` |
  | `collections.isVerified` | boolean | tidak |  |
  | `admins` | object[] | tidak |  |
  | `admins[].id` | string | tidak | UUID v4 |
  | `admins[].name` | string | tidak |  |
  | `admins[].email` | string | tidak | email |
  | `admins[].role` | string | tidak | enum `workspaceRole`: `owner`, `admin`, `staff` |
  | `admins[].addedAt` | string | tidak | ISO 8601 `+07:00` |

  ## Error yang mungkin

  | Kode | Contoh | Kapan |
  | --- | --- | --- |
  | `401` | `common/error-unauthorized.response.json` | Token tidak ada atau kedaluwarsa. |
  | `403` | `common/error-forbidden.response.json` | Akun tidak punya peran untuk konsol ini. |
  | `500` | `common/error-server.response.json` | Kesalahan tak terduga. |

  Semua error memakai envelope yang sama ditambah blok `error`:

  ```json
  { "success": false, "message": "…", "data": null, "meta": null,
    "error": { "code": "VALIDATION_ERROR", "errors": { "email": ["…"] } } }
  ```

  ## Konvensi yang berlaku

  - **Nama field** camelCase.
  - **Uang** di response berbentuk `{ amount: integer, currency: "IDR" }`; di request integer polos.
  - **Instant** (`createdAt`, `paidAt`, `startsAt`, `expiresAt`) ISO 8601 `+07:00`. Jam sesi berulang tetap `"HH:mm"`.
  - **Enum** lowercase. Label berhuruf besar adalah urusan frontend.
  - **Field nullable** tetap dikirim sebagai `null`, jangan dihilangkan.
  - **`roles`** adalah array — satu akun bisa `["participant", "organizer"]`.

  Selengkapnya di `dto/README.md`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
