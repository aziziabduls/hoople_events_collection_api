info:
  name: Checkin Scan
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/teams/check-in/scan"
  auth: inherit
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "code": "SN-KO26-0042",
        "eventId": "f00d1e00-1111-4222-8333-000000000601",
        "scannerId": "gate-1",
        "scannedAt": "2026-03-20T09:15:00+07:00"
      }

runtime:
  assertions:
    - expression: res.status
      operator: lt
      value: "400"
    - expression: res.body.success
      operator: eq
      value: "true"

examples:
  - name: Sukses
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": true,
            "message": "Check-in berhasil",
            "data": {
              "result": "accepted",
              "registrationId": "treg-0001",
              "member": {
                "name": "Adriani Ajeng",
                "employeeId": "SN-0042",
                "department": "People Ops",
                "title": "People Ops Lead"
              },
              "passName": "Plus One",
              "quantity": 2,
              "checkedInAt": "2026-03-20T09:15:00+07:00",
              "scannerId": "gate-1",
              "progress": {
                "checkedIn": 247,
                "registered": 412,
                "rate": 0.599
              }
            },
            "meta": null
          }

docs: |-
  # Checkin Scan

  Scan di gate.

  | | |
  | --- | --- |
  | **Method** | `POST` |
  | **Path** | `/teams/check-in/scan` |
  | **Auth** | Wajib — `Authorization: Bearer <accessToken>` |
  | **Sukses** | `200 OK` |
  | **Sumber** | `dto/teams/checkin-scan.request.json`<br>`dto/teams/checkin-scan.response.json` |

  ## Request body

  `application/json`

  | Field | Tipe | Wajib | Catatan |
  | --- | --- | --- | --- |
  | `code` | string | ya |  |
  | `eventId` | string | ya | UUID v4 |
  | `scannerId` | string | ya |  |
  | `scannedAt` | string | ya | ISO 8601 `+07:00` |


  > Kolom **Wajib** diturunkan dari contoh: field yang bernilai `null`
  > dianggap opsional. Aturan validasi sesungguhnya ditentukan backend.

  ## Response `200`

  Envelope standar:

  ```json
  { "success": true, "message": "…", "data": …, "meta": null }
  ```

  Isi `data`:

  | Field | Tipe | Nullable | Catatan |
  | --- | --- | --- | --- |
  | `result` | string | tidak | enum `scanResult`: `accepted`, `already_used`, `invalid`, `expired`, `wrong_event` |
  | `registrationId` | string | tidak |  |
  | `member` | object | tidak |  |
  | `member.name` | string | tidak |  |
  | `member.employeeId` | string | tidak |  |
  | `member.department` | string | tidak |  |
  | `member.title` | string | tidak |  |
  | `passName` | string | tidak |  |
  | `quantity` | integer | tidak |  |
  | `checkedInAt` | string | tidak | ISO 8601 `+07:00` |
  | `scannerId` | string | tidak |  |
  | `progress` | object | tidak |  |
  | `progress.checkedIn` | integer | tidak |  |
  | `progress.registered` | integer | tidak |  |
  | `progress.rate` | number | tidak |  |

  ## Error yang mungkin

  | Kode | Contoh | Kapan |
  | --- | --- | --- |
  | `401` | `common/error-unauthorized.response.json` | Token tidak ada atau kedaluwarsa. |
  | `422` | `common/error-validation.response.json` | Validasi gagal; pesan per field ada di `error.errors`. |
  | `409` | `organizer/checkin-scan-rejected.response.json` | Tiket sudah dipakai check-in. |
  | `403` | `common/error-forbidden.response.json` | Akun tidak punya peran untuk konsol ini. |
  | `500` | `common/error-server.response.json` | Kesalahan tak terduga. |

  Semua error memakai envelope yang sama ditambah blok `error`:

  ```json
  { "success": false, "message": "…", "data": null, "meta": null,
    "error": { "code": "VALIDATION_ERROR", "errors": { "email": ["…"] } } }
  ```

  ## Konvensi yang berlaku

  - **Nama field** camelCase.
  - **Uang** di response berbentuk `{ amount: integer, currency: "IDR" }`; di request integer polos.
  - **Instant** (`createdAt`, `paidAt`, `startsAt`, `expiresAt`) ISO 8601 `+07:00`. Jam sesi berulang tetap `"HH:mm"`.
  - **Enum** lowercase. Label berhuruf besar adalah urusan frontend.
  - **Field nullable** tetap dikirim sebagai `null`, jangan dihilangkan.
  - **`roles`** adalah array — satu akun bisa `["participant", "organizer"]`.

  Selengkapnya di `dto/README.md`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
