info:
  name: Event Trending This Week
  type: http
  seq: 18

http:
  method: GET
  url: "{{baseUrl}}/events/trending-this-week"
  auth: none

runtime:
  assertions:
    - expression: res.status
      operator: lt
      value: "400"
    - expression: res.body.success
      operator: eq
      value: "true"

docs: |-
  # Event Trending This Week

  Endpoint rail event "Trending this week".

  | | |
  | --- | --- |
  | **Method** | `GET` |
  | **Path** | `/events/trending-this-week` |
  | **Auth** | Tidak perlu (publik) |
  | **Sumber** | `dto/participant/catalog/event-trending-this-week.response.json` |

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
