info:
  name: Booking Cancel
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/bookings/{id}/cancel"
  auth: inherit
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "reason": "Ada agenda mendadak",
        "requestRefund": true
      }

runtime:
  assertions:
    - expression: res.status
      operator: lt
      value: "400"
    - expression: res.body.success
      operator: eq
      value: "true"

examples:
  - name: Sukses
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": true,
            "message": "Pesanan berhasil dibatalkan",
            "data": {
              "id": "b0071e5c-1111-4d2e-8a3f-000000000301",
              "orderId": "ORD-20260314-0001",
              "status": "cancelled",
              "paymentStatus": "refunded",
              "cancelledAt": "2026-03-15T08:12:00+07:00",
              "cancellationReason": "Ada agenda mendadak",
              "refund": {
                "total": {
                  "amount": 251520,
                  "currency": "IDR"
                },
                "estimatedArrival": "2026-03-19T23:59:00+07:00",
                "destination": "BCA ****7890"
              }
            },
            "meta": null
          }

docs: |-
  # Booking Cancel

  Batalkan + info refund.

  | | |
  | --- | --- |
  | **Method** | `POST` |
  | **Path** | `/bookings/{id}/cancel` |
  | **Auth** | Wajib — `Authorization: Bearer <accessToken>` |
  | **Sukses** | `200 OK` |
  | **Sumber** | `dto/participant/bookings/booking-cancel.request.json`<br>`dto/participant/bookings/booking-cancel.response.json` |

  ## Path parameter

  | Nama | Catatan |
  | --- | --- |
  | `{id}` | UUID entitas. |

  ## Request body

  `application/json`

  | Field | Tipe | Wajib | Catatan |
  | --- | --- | --- | --- |
  | `reason` | string | ya |  |
  | `requestRefund` | boolean | ya |  |


  > Kolom **Wajib** diturunkan dari contoh: field yang bernilai `null`
  > dianggap opsional. Aturan validasi sesungguhnya ditentukan backend.

  ## Response `200`

  Envelope standar:

  ```json
  { "success": true, "message": "…", "data": …, "meta": null }
  ```

  Isi `data`:

  | Field | Tipe | Nullable | Catatan |
  | --- | --- | --- | --- |
  | `id` | string | tidak | UUID v4 |
  | `orderId` | string | tidak |  |
  | `status` | string | tidak | enum `bookingStatus`: `pending`, `confirmed`, `completed`, `cancelled`, `refunded` |
  | `paymentStatus` | string | tidak | enum `paymentStatus`: `pending`, `paid`, `failed`, `refunded`, `covered` |
  | `cancelledAt` | string | tidak | ISO 8601 `+07:00` |
  | `cancellationReason` | string | tidak |  |
  | `refund` | object | tidak |  |
  | `refund.total` | money | tidak | `{ amount: integer, currency: "IDR" }` |
  | `refund.estimatedArrival` | string | tidak | ISO 8601 `+07:00` |
  | `refund.destination` | string | tidak |  |

  ## Error yang mungkin

  | Kode | Contoh | Kapan |
  | --- | --- | --- |
  | `401` | `shared/common/error-unauthorized.response.json` | Token tidak ada atau kedaluwarsa. |
  | `404` | `shared/common/error-not-found.response.json` | Id atau slug tidak ditemukan. |
  | `422` | `shared/common/error-validation.response.json` | Validasi gagal; pesan per field ada di `error.errors`. |
  | `500` | `shared/common/error-server.response.json` | Kesalahan tak terduga. |

  Semua error memakai envelope yang sama ditambah blok `error`:

  ```json
  { "success": false, "message": "…", "data": null, "meta": null,
    "error": { "code": "VALIDATION_ERROR", "errors": { "email": ["…"] } } }
  ```

  ## Konvensi yang berlaku

  - **Nama field** camelCase.
  - **Uang** di response berbentuk `{ amount: integer, currency: "IDR" }`; di request integer polos.
  - **Instant** (`createdAt`, `paidAt`, `startsAt`, `expiresAt`) ISO 8601 `+07:00`. Jam sesi berulang tetap `"HH:mm"`.
  - **Enum** lowercase. Label berhuruf besar adalah urusan frontend.
  - **Field nullable** tetap dikirim sebagai `null`, jangan dihilangkan.
  - **`roles`** adalah array — satu akun bisa `["participant", "organizer"]`.

  Selengkapnya di `dto/README.md`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
