info:
  name: Checkin Summary
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/organizer/check-in/summary?sessionId=ses-0001-4aaa-8bbb-000000000001"
  auth: inherit
  params:
    - name: sessionId
      value: ses-0001-4aaa-8bbb-000000000001
      type: query
      description: UUID sesi.

runtime:
  assertions:
    - expression: res.status
      operator: lt
      value: "400"
    - expression: res.body.success
      operator: eq
      value: "true"

examples:
  - name: Sukses
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "success": true,
            "message": "Ringkasan check-in berhasil diambil",
            "data": {
              "sessionId": "ses-0001-4aaa-8bbb-000000000001",
              "experienceTitle": "Morning Yoga Flow",
              "startsAt": "2026-03-16T07:00:00+07:00",
              "booked": 24,
              "checkedIn": 18,
              "noShow": 2,
              "pending": 4,
              "checkInRate": 0.75,
              "recentScans": [
                {
                  "ticketCode": "HPL-8K2M-4QX7-1",
                  "name": "Adriani Ajeng",
                  "checkedInAt": "2026-03-16T06:52:00+07:00"
                },
                {
                  "ticketCode": "HPL-2M9X-7KL3-1",
                  "name": "Sinta Wijaya",
                  "checkedInAt": "2026-03-16T06:49:00+07:00"
                }
              ]
            },
            "meta": null
          }

docs: |-
  # Checkin Summary

  Progres check-in sesi berjalan.

  | | |
  | --- | --- |
  | **Method** | `GET` |
  | **Path** | `/organizer/check-in/summary` |
  | **Auth** | Wajib — `Authorization: Bearer <accessToken>` |
  | **Sukses** | `200 OK` |
  | **Sumber** | `dto/organizer/checkin-summary.response.json` |

  ## Query parameter

  | Nama | Contoh | Catatan |
  | --- | --- | --- |
  | `sessionId` | `ses-0001-4aaa-8bbb-000000000001` | UUID sesi. |

  ## Request body

  Tidak ada.

  ## Response `200`

  Envelope standar:

  ```json
  { "success": true, "message": "…", "data": …, "meta": null }
  ```

  Isi `data`:

  | Field | Tipe | Nullable | Catatan |
  | --- | --- | --- | --- |
  | `sessionId` | string | tidak |  |
  | `experienceTitle` | string | tidak |  |
  | `startsAt` | string | tidak | ISO 8601 `+07:00` |
  | `booked` | integer | tidak |  |
  | `checkedIn` | integer | tidak |  |
  | `noShow` | integer | tidak |  |
  | `pending` | integer | tidak |  |
  | `checkInRate` | number | tidak |  |
  | `recentScans` | object[] | tidak |  |
  | `recentScans[].ticketCode` | string | tidak |  |
  | `recentScans[].name` | string | tidak |  |
  | `recentScans[].checkedInAt` | string | tidak | ISO 8601 `+07:00` |

  ## Error yang mungkin

  | Kode | Contoh | Kapan |
  | --- | --- | --- |
  | `401` | `common/error-unauthorized.response.json` | Token tidak ada atau kedaluwarsa. |
  | `403` | `common/error-forbidden.response.json` | Akun tidak punya peran untuk konsol ini. |
  | `500` | `common/error-server.response.json` | Kesalahan tak terduga. |

  Semua error memakai envelope yang sama ditambah blok `error`:

  ```json
  { "success": false, "message": "…", "data": null, "meta": null,
    "error": { "code": "VALIDATION_ERROR", "errors": { "email": ["…"] } } }
  ```

  ## Konvensi yang berlaku

  - **Nama field** camelCase.
  - **Uang** di response berbentuk `{ amount: integer, currency: "IDR" }`; di request integer polos.
  - **Instant** (`createdAt`, `paidAt`, `startsAt`, `expiresAt`) ISO 8601 `+07:00`. Jam sesi berulang tetap `"HH:mm"`.
  - **Enum** lowercase. Label berhuruf besar adalah urusan frontend.
  - **Field nullable** tetap dikirim sebagai `null`, jangan dihilangkan.
  - **`roles`** adalah array — satu akun bisa `["participant", "organizer"]`.

  Selengkapnya di `dto/README.md`.

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
